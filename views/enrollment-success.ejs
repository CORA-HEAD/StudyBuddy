<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enrollment Successful</title>
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }

        /* Message Box Styles */
        .message-box {
            text-align: center;
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        h1 {
            color: #007bff; /* Set heading color as discussed */
            font-size: 2em;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2em;
            color: #333;
        }
    </style>
    <script>
        // Redirect after 3 seconds
        setTimeout(() => {
            // Redirect to the first course in the course_ids array
            window.location.href = `/course/<%= course_ids[0] %>?user_id=<%= user_id %>`;
        }, 3000); 
    </script>
</head>
<body>
    <div class="message-box">
        <h1><%= message %></h1>
        <p>You will be redirected to the course page shortly.</p>
    </div>
</body>
</html>
